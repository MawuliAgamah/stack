[project]
name = "stack"
version = "0.1.0"
description = "A python based framework for scaffolding full stack applications"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ['click']

[project.scripts]
stack = "stack.cli:main"